import { LogSourceParams } from './log-source-params';
export class LogSourceParamsFuncHandler extends LogSourceParams {
    handle(_functionInfo, valOrFunc) {
        valOrFunc();
        return true;
    }
}
