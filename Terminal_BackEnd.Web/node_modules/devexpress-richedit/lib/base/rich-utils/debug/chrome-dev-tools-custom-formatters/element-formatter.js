import { BaseFormatter } from './base-formatter';
import { BaseElement } from './elements';
export class CCF_ElementFormatter extends BaseFormatter {
    isHandleObject(obj) {
        return obj instanceof BaseElement;
    }
    getShortDescription(_config) {
        return this.curr;
    }
}
CCF_ElementFormatter._foo = BaseFormatter.addToFormattersList(new CCF_ElementFormatter());
